import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.whatmars.app',
  appName: 'WhatMars',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
